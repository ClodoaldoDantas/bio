---
const response = await fetch("https://api.github.com/users/clodoaldodantas");
const user = await response.json();
---

<header class="header">
  <img
    src="https://api.dicebear.com/8.x/fun-emoji/svg?size=96&seed=Patches&radius=10"
    alt="Avatar"
  />
  <h1>{user.name}</h1>
  <p>{user.bio}</p>
</header>

<style lang="scss">
  .header {
    text-align: center;

    img {
      margin: 0 auto 1.25rem;
    }

    h1 {
      font-size: 2rem;
      font-weight: 600;
    }

    p {
      font-size: 1.125rem;
    }
  }
</style>
