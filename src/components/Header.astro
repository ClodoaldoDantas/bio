---
const response = await fetch("https://api.github.com/users/clodoaldodantas");
const user = await response.json();
---

<header class="header">
  <img
    src="https://api.dicebear.com/8.x/icons/svg?size=96&radius=10&icon=controller&backgroundColor=8b5cf6"
    alt="Avatar"
  />
  <h1>{user.name}</h1>
  <p>{user.bio}</p>
</header>

<style lang="scss">
  .header {
    text-align: center;

    img {
      margin: 0 auto 1.25rem;
    }

    h1 {
      font-size: 2rem;
      font-weight: 600;
    }

    p {
      font-size: 1.125rem;
    }
  }
</style>
